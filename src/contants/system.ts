export enum PROTECT_STATUS {
  NORMAL = 0,
  BUY_WIN = 1,
  SELL_WIN = -1,
}

export enum TYPE_WIN {
  NEUTRAL = 0,
  BUY = 1,
  SELL = 2,
}

export enum TypeUser {
  REAL = 0,
  DEMO = 1,
  EXPERT = 2,
  COPY_TRADE = 3,
}

export enum TYPE_ORDER {
  BUY = 1,
  SELL = 2,
}
